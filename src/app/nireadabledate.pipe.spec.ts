import { NireadabledatePipe } from './nireadabledate.pipe';

describe('NireadabledatePipe', () => {
  it('create an instance', () => {
    const pipe = new NireadabledatePipe();
    expect(pipe).toBeTruthy();
  });
});
