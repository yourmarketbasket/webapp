import { NiFilePondDirective } from './ni-file-pond.directive';

describe('NiFilePondDirective', () => {
  it('should create an instance', () => {
    const directive = new NiFilePondDirective();
    expect(directive).toBeTruthy();
  });
});
